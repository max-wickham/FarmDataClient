<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="FarmData.Pages.AddCropPage">
    <ContentPage.Content>
        <ScrollView>
            <StackLayout>
                <Grid>
                    <StackLayout x:Name="View">
                        <Picker x:Name="CropPicker"/>
                        <Entry x:Name="OtherEntry"/>
                        <Picker x:Name="SizePicker"/>
                    </StackLayout>
                </Grid>
                <Grid>
                    <StackLayout x:Name="View2">
                        <Button x:Name="Save" Text="{x:Static resx:Strings.Save}" Clicked="Save_Clicked"/>
                    </StackLayout>
                </Grid>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>